package freshStart

import io.kotlintest.specs.BehaviorSpec

class TestThatMaitreD : BehaviorSpec ({
    given("a MaitreD"){
      //  val maitreD = MaitreD()
        `when`("make a reservation")
        {
            then("it should be accepted"){

            }
        }
    }
})